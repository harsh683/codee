<h2>Loan Prediction.</h2>

<h3>Files:</>

#loan prediction model : A notebook in which data is read and then cleaned and the model is trained.

#loan prediction : A notebook where that model is used to predict the loan status using our model.

#loan.csv : data csv file to train model.

#cleaned_loan_data.csv : data with no missing values. (can be used directly too)

#rf_model : This is the model that is created into 'loan prediction model' and used to predict in 'loan prediction'.
